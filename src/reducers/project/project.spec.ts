// import {fromJS} from 'immutable';
// import {expect} from 'chai';
// import * as Action from '../../../lib/actions/actions';
// import {reducer} from '../../../lib/_base';
//
// xdescribe('project reducer', () => {
//
//   describe('SET_PROJECT', () => {
//     it('should set the file path on SET_PROJECT', () => {
//       const state = fromJS({ project: { filePath: null } });
//       const filePath = './path/to/file.js';
//       const action = Action.setProject(filePath);
//       expect(reducer(state, action)).to.equal(fromJS({ project: { filePath } }));
//     });
//   });
//
// });

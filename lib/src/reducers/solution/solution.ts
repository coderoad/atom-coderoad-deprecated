import * as Type from '../../actions/actionTypes';

export default function solutionReducer(solution = [], action): string {
  switch (action.type) {
    // case Type.SHOW_SOLUTION:
    default:
      return solution;
  }
}
